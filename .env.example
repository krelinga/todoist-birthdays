# Copy to .env and fill in. docker-compose.yml reads these automatically.
TODOIST_API_TOKEN=your-todoist-api-token-here
TODOIST_PROJECT_NAME=Birthdays
TZ=America/Chicago
RUN_AT=08:00
METRICS_PORT=9090
# TODOIST_MAX_ATTEMPTS=3  # optional: retry attempts per Todoist API call
